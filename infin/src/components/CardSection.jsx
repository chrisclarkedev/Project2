import React from 'react';
import Form from './Form';
import { getHero } from "./Search"
import { Link } from 'react-router-dom';

export default function CardSection() {
  return (
    <section id="card-section">
      <div id="cardSetup">
      </div>
    </section>
  )
}